services:
  update-server:
    build:
      context: .
      dockerfile: Dockerfile.update-server
    # tini as PID 1 reaps zombies (adb fork-server children orphaned via
    # double-fork). Belt-and-suspenders alongside the in-process subprocess
    # discipline in update-server.py.
    init: true
    network_mode: host
    volumes:
      # Mount the APK output directory so the server always serves the latest build
      - ./app/build/outputs/apk/debug:/app/app/build/outputs/apk/debug:ro
      # Mount the build-tools for aapt2 (reads version directly from APK files)
      - ./android-sdk/build-tools:/app/android-sdk/build-tools:ro
      # Mount the ADB platform-tools for device management
      - ./android-sdk/platform-tools:/app/android-sdk/platform-tools:ro
      # Mount ADB keys from project directory for portability
      - ./android-sdk/keys:/root/.android:rw
      # Persist logs and state data
      - server-data:/app/data
      # VERSION as the single source of truth for /healthz + the UI version.
      - ./VERSION:/app/VERSION:ro
      # Mount D-Bus socket for avahi-browse mDNS resolution
      - /var/run/dbus:/var/run/dbus:ro
    environment:
      - HOME=/root
      # Disable adb's built-in mDNS discovery. The openscreen-based
      # implementation busy-loops on the UDP/5353 multicast socket on
      # this host (~10k syscalls/sec → ~45% CPU). We don't need it —
      # update-server.py resolves _adb-tls-connect._tcp via avahi-browse.
      - ADB_MDNS=0
    restart: unless-stopped

volumes:
  server-data:
